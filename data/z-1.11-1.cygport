CATEGORY=Private
SUMMARY=${PN}
ARCH=noarch
GIT_URI=https://github.com/rupa/${PN}.git
GIT_TAG=v${PV}
inherit git

src_compile() {
    cd ${S}
    lndirs
}

src_install() {
    cd ${B}
    newbin z.sh z
    doman z.1
}
