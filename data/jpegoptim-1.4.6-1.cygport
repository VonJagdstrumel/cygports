CATEGORY=Private
SUMMARY=${PN}
DEPEND=libjpeg-devel
GIT_URI=https://github.com/tjko/${PN}.git
GIT_TAG=RELEASE.${PV}
inherit git

src_compile() {
    cd ${S}
    lndirs
    cd ${B}
    cygconf
    cygmake
}
