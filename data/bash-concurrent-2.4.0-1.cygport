CATEGORY='Libs Shells'
SUMMARY=${PN}
ARCH=noarch
GIT_URI=https://github.com/themattrix/${PN}.git
GIT_TAG=${PV}
inherit git

src_compile() {
    cd ${S}
    lndirs
}

src_install() {
    cd ${B}
    dodir /usr/lib/${PN}
    insinto /usr/lib/${PN}
    doins concurrent.lib.sh
}
