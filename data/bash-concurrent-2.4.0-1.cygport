CATEGORY=Private
SUMMARY=${PN}
ARCH=noarch
GIT_URI=https://github.com/themattrix/${PN}.git
GIT_TAG=${PV}
inherit git

src_compile() {
    cd ${S}
    lndirs
}

src_install() {
    cd ${B}
    dodir /usr/lib/bash-concurrent
    insinto /usr/lib/bash-concurrent
    doins concurrent.lib.sh
}
