CATEGORY='Graphics Utils'
GIT_URI=https://github.com/tjko/${PN}.git
GIT_TAG=RELEASE.${PV}
DEPEND=libjpeg-devel
. ${top}/../base.cyg

src_compile() {
    cd ${S}
    lndirs
    cd ${B}
    cygconf
    cygmake
}
