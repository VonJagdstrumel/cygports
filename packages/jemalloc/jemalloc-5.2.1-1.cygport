CATEGORY=Devel
GIT_URI=https://github.com/jemalloc/${PN}.git
GIT_TAG=${PV}
DEPEND=libxslt
CPPFLAGS=-D_GNU_SOURCE
NO_AUTOHEADER=true
. ${top}/../base.cyg

src_install() {
    cd ${B}
    cyginstall -j1
}
