CATEGORY=Devel
SRC_URI=https://github.com/jemalloc/${PN}/archive/${PV}.tar.gz
DEPEND=libxslt
CPPFLAGS=-D_GNU_SOURCE
NO_AUTOHEADER=true
. ${top}/../base.cyg

src_install() {
    cd ${B}
    cyginstall -j1
}
