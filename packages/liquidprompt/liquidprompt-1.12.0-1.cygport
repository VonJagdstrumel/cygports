CATEGORY=Shells
GIT_URI=https://github.com/nojhan/${PN}.git
GIT_TAG=v${PV}
ARCH=noarch
. ${top}/../base.cyg

src_compile() {
    lndirs
}

src_install() {
    cd ${B}
    dobin liquidprompt
    insinto /etc
    newins liquidpromptrc-dist liquidpromptrc
}
