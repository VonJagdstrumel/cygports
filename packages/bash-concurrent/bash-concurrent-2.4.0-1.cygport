CATEGORY='Libs Shells'
GIT_URI=https://github.com/themattrix/${PN}.git
GIT_TAG=${PV}
ARCH=noarch
. ${top}/../base.cyg

src_compile() {
    cd ${S}
    lndirs
}

src_install() {
    cd ${B}
    dodir /usr/lib/${PN}
    insinto /usr/lib/${PN}
    doins concurrent.lib.sh
}
