CATEGORY='Shells Utils'
GIT_URI=https://github.com/denisidoro/${PN}.git
GIT_TAG=v${PV}
ARCH=noarch
. ${top}/../base.cyg

src_compile() {
    cd ${S}
    lndirs
}

src_install() {
    cd ${B}
    dodir /usr/bin
    ./scripts/install ${D}/usr/bin
}
